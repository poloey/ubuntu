# to use single quote instead of double quote in emmet html 

* make a folder in home directory name `emmet`
* make a `profiles.json` file inside `emmet` folder.
* write following code
~~~json
{
	"html": {
	  "attr_quotes": "single"
	}
}

~~~